RAILWAY UPDATE CHECKLIST
========================

Your PC has panel v3. Railway is still OLD because GitHub did not get the new files yet.

STEP 1 — Push to GitHub (GitHub Desktop)
----------------------------------------
1. Open GitHub Desktop
2. Repository: AdminPOS (folder: Documents\AdminPOS)
3. You MUST see changed files including:
   license-server/public/index.html
   license-server/src/index.ts
   license-server/package.json
   license-server/scripts/copy-public.mjs
4. Summary: Fix license panel v3
5. Commit to main
6. Push origin (top bar "Push origin" or Repository → Push)

STEP 2 — Verify on github.com (IMPORTANT)
-----------------------------------------
Open in browser:
https://github.com/elgranprincipebna-cpu/AdminPOS/tree/main/license-server/public

You must see index.html. Click it — search for "Panel v3" or "fetchJson".
If folder missing or old text → push did not work. Repeat Step 1.

STEP 3 — Railway settings
-------------------------
railway.app → your project → license-server service → Settings:

  Root Directory:  license-server
  Branch:          main

Save if you changed anything.

STEP 4 — Force redeploy
-----------------------
Deployments tab → ⋮ menu on latest → Redeploy
OR click "Deploy" / "Redeploy" button.

Wait until status: SUCCESS (green).

STEP 5 — Confirm live site
--------------------------
Open: https://license-server-production-2c54.up.railway.app/

Browser tab title must say:  Licencias (v3)
Page must say:               Panel v3

Health: https://license-server-production-2c54.up.railway.app/health
Must show: "panelVersion": 3

If still old → Railway is not pulling from GitHub. Check Step 3 Root Directory.
